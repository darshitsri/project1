apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ngress-ervice
  annotations:
#   Ingress Core Settings
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/ achene: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - http:
        paths:
          - path: /
            patType: Prefix 
            backend:
              service:
                name: springboot-app
                port:
                  number: 8085
